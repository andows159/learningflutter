//anotation for string and types on dart
main() {
  //after infered the type automatically by compiler, will impossible change it
  var number1 = 2;
  print(number1.runtimeType);
  //the string system runs like in python, or at least similar
}
